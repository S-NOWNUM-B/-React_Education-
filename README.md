# React Learning Path

Репозиторий для изучения React с теоретическими материалами и практическими заданиями.

## Структура

### 📚 `theory/`

Папка с теоретическими материалами и конспектами:

- Основные концепции React
- Компоненты и JSX
- State и Props
- Hooks
- Routing
- State Management
- И другие темы...

### 💻 `practice/`

Практические задания и проекты:

#### `exercises/`

Небольшие упражнения для закрепления навыков:

- Упражнение 1-10: Основы
- Упражнение 11-20: Компоненты
- Упражнение 21-30: Hooks
- И т.д.

#### `projects/`

Полноценные проекты для применения знаний. **Каждый проект имеет свой собственный:**

- `package.json` с зависимостями
- `vite.config.ts` конфигурация
- `tsconfig.json` для TypeScript
- `.eslintrc.cjs` для ESLint

## 📁 Корень проекта

Корень содержит только:

- **README.md** - документация
- **.gitignore** - конфиги Git
- **.vscode/** - общие настройки VS Code для всех проектов

> ⚠️ **Важно**: В корне **нет** `package.json` с зависимостями, `vite`, `tsconfig` и других конфигов. Каждый проект полностью самостоятелен!

## Быстрый старт

### Для упражнений:

```bash
cd practice/exercises/exercise-1
npm install
npm run dev
```

### Для проектов:

```bash
cd practice/projects/todo-app
npm install
npm run dev
```

## Требуемые инструменты (глобально)

- Node.js (v16+)
- npm или yarn
- VS Code (рекомендуется)

## Как использовать

1. **Для теории**: Открыть нужный файл в папке `theory/`
2. **Для практики**: Перейти в папку конкретного упражнения или проекта
3. **Установка зависимостей**: `npm install` в папке проекта (не в корне!)
4. **Запуск dev сервера**: `npm run dev` внутри проекта

## Команды для каждого проекта

```bash
npm run dev        # Запуск dev сервера
npm run build      # Production сборка
npm run preview    # Просмотр сборки
npm run type-check # Проверка типов TypeScript
npm run lint       # ESLint проверка
```

## Progress Tracking

Отмечай завершённые обучающие модули и проекты по мере прогресса!

---

**Структура оптимизирована для обучения**: каждый проект полностью независим и готов к работе! 🚀
